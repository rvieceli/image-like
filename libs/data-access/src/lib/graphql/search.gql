query search($query: String!, $page: Int) {
  search(query: $query, page: $page) {
    total
    total_pages
    results {
      id
      author
      url
      link
      height
      width
      likesCount
      likedBy {
        id
        name
      }
    }
  }
}
